npm expo start will start the app, scan the qr in the app installed with the same apk as the sdk of expo, and be in the same network.

install the dependencies like nativewind by refering to the docs, and reinstall node modules and do npx expo start --clear
